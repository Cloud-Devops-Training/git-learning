services:
  jenkins:
    container_name: jenkins
    image: jenkins/jenkins
    ports:
      - "8080:8080"
    volumes:
      - $PWD/project_home:/var/jenkins_home
    networks:
      - net
networks:
  net:
